---
import TelemetryPanel from './TelemetryPanel.astro';
---

<section class="hero">
  <div class="hero-content">
    <div class="hero-label">
      <span class="hero-label-line"></span>
      <span class="hero-label-text">Cognitive Infrastructure</span>
    </div>

    <h1 class="hero-headline">
      <span class="hero-headline-1">Know thyself.</span>
      <span class="hero-headline-2">Empirically.</span>
    </h1>

    <p class="hero-intro">
      Phronos is an observatory for human cognition in the age of artificial intelligence. 
      We develop methods to explore cognitive phenotypes, track human reasoning over time, 
      and map the interface between human and machine intelligence.
    </p>
  </div>

  <div class="hero-telemetry">
    <TelemetryPanel />
  </div>
</section>

<style>
  .hero {
    display: grid;
    grid-template-columns: 1fr 340px;
    gap: 4rem;
    align-items: center;
    padding: 5rem 5% 4rem;
    max-width: 1200px;
    margin: 0 auto;
  }

  .hero-content {
    max-width: 540px;
  }

  .hero-label {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    margin-bottom: 1.5rem;
  }

  .hero-label-line {
    width: 40px;
    height: 1px;
    background: var(--faded);
  }

  .hero-label-text {
    font-family: var(--font-mono);
    font-size: var(--text-sm);
    text-transform: uppercase;
    letter-spacing: 2px;
    color: var(--faded);
  }

  .hero-headline {
    margin-bottom: 1.75rem;
  }

  .hero-headline-1,
  .hero-headline-2 {
    display: block;
    font-family: var(--font-serif);
    font-size: clamp(2.5rem, 5.5vw, 3.75rem);
    font-weight: 300;
    line-height: 1.1;
    letter-spacing: -0.02em;
  }

  .hero-headline-1 {
    color: var(--ink);
  }

  .hero-headline-2 {
    color: var(--gold);
    font-style: italic;
  }

  .hero-intro {
    font-family: var(--font-serif);
    font-size: 1.15rem;
    line-height: 1.7;
    color: var(--ink);
    max-width: 540px;
  }

  .hero-telemetry {
    display: flex;
    justify-content: flex-end;
  }

  @media (max-width: 900px) {
    .hero {
      display: block;
    }

    .hero-telemetry {
      margin-top: 2.5rem;
    }
  }

  @media (max-width: 600px) {
    .hero {
      padding: 3rem 5% 2.5rem;
    }

    .hero-headline-1,
    .hero-headline-2 {
      font-size: clamp(2rem, 8vw, 3rem);
    }
  }
</style>

