---
---

<div class="dispatch-subscribe">
  <div class="dispatch-subscribe-content">
    <p class="dispatch-subscribe-text">Get dispatches delivered to your inbox.</p>
  </div>
  <a href="https://phronos.substack.com/subscribe" target="_blank" rel="noopener" class="dispatch-subscribe-button">
    Subscribe →
  </a>
</div>

<style>
  .dispatch-subscribe {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1.5rem 2rem;
    margin: 3rem 5%;
    max-width: 800px;
    background: var(--white);
    border: 1px solid var(--ink);
  }

  .dispatch-subscribe-text {
    font-family: var(--font-serif);
    font-size: 1.1rem;
    color: var(--ink);
    margin: 0;
  }

  .dispatch-subscribe-button {
    font-family: var(--font-mono);
    font-size: var(--text-sm);
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 1px;
    color: var(--white);
    background: var(--ink);
    border: 1px solid var(--ink);
    padding: 0.75rem 1.5rem;
    text-decoration: none;
    transition: all var(--transition-default);
    white-space: nowrap;
  }

  .dispatch-subscribe-button:hover {
    background: var(--gold);
    border-color: var(--gold);
    color: var(--white);
  }

  @media (max-width: 767px) {
    .dispatch-subscribe {
      flex-direction: column;
      gap: 1rem;
      text-align: center;
      margin: 2rem 5%;
    }
  }
</style>

